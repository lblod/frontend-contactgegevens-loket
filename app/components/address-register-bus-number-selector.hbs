<PowerSelect
  @disabled={{@disabled}}
  @searchEnabled={{true}}
  @placeholder={{@placeholder}}
  @selected={{@selected}}
  @renderInPlace={{false}}
  @options={{@options}}
  @onChange={{@onChange}}
  @loadingMessage="Aan het laden..."
  as |address|
>
  {{#if address.busNumber}}
    Bus
    {{address.busNumber}}
  {{else}}
    Geen busnummer
  {{/if}}
</PowerSelect>