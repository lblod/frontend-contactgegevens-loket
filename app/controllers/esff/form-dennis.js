import Controller from '@ember/controller';
import { action } from '@ember/object';

export default class CoreDataController extends Controller {
  @action
  validateForm() {
    console.log('qsodfiqsodfiqs');
  }
}
